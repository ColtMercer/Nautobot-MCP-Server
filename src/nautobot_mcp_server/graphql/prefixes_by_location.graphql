query PrefixesByLocation($name: String!) {
  prefixes(locations: [$name]) {
    prefix
    status {
      name
    }
    role {
      name
    }
    description
    locations {
      name
    }
  }
}


